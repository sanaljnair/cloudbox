    version: '3.8' # Use a compatible Docker Compose version
    services:
      web:
        image: '594154735784.dkr.ecr.us-east-1.amazonaws.com/upgrad-repo:${IMAGE_TAG}' # Use the ECR image and tag
        ports:
          - '80:80' # Map port 80 on the host to port 80 in the container
        restart: always